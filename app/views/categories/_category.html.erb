<div id="category-<%= category.id %>" class="category">
  <div class="row">
    <div class="col-md-4">
      <strong><span><%= link_to category.name, category %></span></strong>
      | <%= category.words.count %> <%= t "words" %>
    </div>
    <div class="col-md-8">
      <em><span class="content"><%= category.description %></span></em>
    </div>
  </div>
</div>